#include "mtpipeline.h"

using namespace Tempest;
using namespace Tempest::Detail;

MtPipeline::MtPipeline(const RenderState &rs, size_t stride,
                       const NsPtr &vert, const NsPtr &frag) {

  // [MTLRenderPipelineDescriptor ];
  }
